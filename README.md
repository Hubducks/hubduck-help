# HubDuck Help

This repo hosts the HubDuck help site (GitHub Pages) using Jekyll + Just-the-Docs.

## Local Preview

```bash
bundle install
bundle exec jekyll serve --livereload
```

Then open `http://localhost:4000`.

## Deploy

Deployment is via GitHub Pages (Actions workflow in `.github/workflows/pages.yml`).

